# Mobile Apps Placeholder

Directories seeded for future native clients:
- `ios/` – native iOS project workspace
- `android/` – native Android module
- `shared/` – cross-platform utilities (React Native/KMM/etc.)

Populate when mobile roadmap activates.
