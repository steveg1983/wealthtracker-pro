npm run test:supabase-smoke || exit 1
npm run test:realtime || exit 1
npm run test:coverage || exit 1
# Statements threshold recalibrated 65→63 on 2026-06-11: deleting the dead
# Redux store removed ~6.5K lines of heavily-tested dead code, which had been
# inflating the global ratio. Live-code coverage did not regress (31 tests were
# added the same day). Raise this as the BLOCKER #4 testing effort lands.
node scripts/verify-coverage-threshold.mjs coverage/coverage-final.json --statements=63 --branches=55 || exit 1
