# Sentry Configuration Example
# Copy this to .env.local and fill in your Sentry DSN

# Enable error tracking
VITE_ENABLE_ERROR_TRACKING=true

# Your Sentry DSN (get this from https://sentry.io)
VITE_SENTRY_DSN=https://YOUR_PUBLIC_KEY@sentry.io/YOUR_PROJECT_ID

# Optional: Send errors in development (default: false)
VITE_SENTRY_SEND_IN_DEV=false

# Optional: Environment name (default: development/production based on NODE_ENV)
# VITE_APP_ENV=staging