// Add this import at the top:
import { formatCurrency } from '../utils/currency-decimal';

// Then replace legacy currency snippets that manually round values with:
// {formatCurrency(someValue)}

// For example, change:
<p className="text-2xl font-bold">£{totalBalance}</p>

// To:
<p className="text-2xl font-bold">{formatCurrency(totalBalance)}</p>
