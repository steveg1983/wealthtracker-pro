/**
 * DashboardRedux.tsx has been intentionally removed. The legacy Redux dashboard
 * was superseded by DashboardNew/DashboardDraggable. The old implementation
 * was largely dead code (unused imports/hooks) and generated dozens of lint
 * warnings. If we ever need to resurrect it, refer to the VCS history prior to
 * 2025-10-10.
 */
export default function DashboardRedux(): null {
  console.warn('DashboardRedux has been archived; use DashboardNew instead.');
  return null;
}
