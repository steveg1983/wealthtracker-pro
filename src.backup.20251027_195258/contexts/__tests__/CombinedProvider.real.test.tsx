import { describe, it } from 'vitest';

describe.skip('CombinedProvider real-database integration', () => {
  it('is pending Supabase harness coverage alongside AppProvider', () => {
    // Tracking ticket: Phase 1 Supabase test harness (see docs/recovery-status.md)
  });
});
