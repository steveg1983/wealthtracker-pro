import { describe, it } from 'vitest';

describe.skip('AppProvider real-database integration', () => {
  it('awaits Supabase harness support before enabling live tests', () => {
    // Tracking ticket: Phase 1 Supabase test harness (see docs/recovery-status.md)
  });
});
