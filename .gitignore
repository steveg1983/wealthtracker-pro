node_modules
coverage/
coverage*.json
coverage.lcov
logs/
dist/
.DS_Store
coverage-final.json
# Backup directories and files — never tracked (CLAUDE.md Rule #2).
# NOTE: ignore rules don't untrack already-committed files; the 2026-06-13
# hygiene sweep (audit finding #37) removed ~8.7k pre-ignore stragglers.
WealthTracker-Backups/
src.backup.*
src-backup-*
*.backup
*.backup.*
*.original
*.bak

# Environment files — never commit secrets
.env
.env.*
!.env.example
backups/
test-results/
playwright-report/

# MS Money local import (never commit real financial data)
.mny-local/
public/mny-local-seed.json
*.mny
**/mny-local-*.json
mny-local-seed.json
